import React from "react";

const PersonalDetailInfo = ({ info1, info2 }) => {
  return (
    <p>
      <span>{info1}</span>
      <span>{info2}</span>
    </p>
  );
};

export default PersonalDetailInfo;
